# Báo cáo trình chiếu / video (abstracts)

Thư mục này dùng để lưu tài liệu trình bày trước Hội đồng, ví dụ:

- File `.ppt` / `.pptx` (bảo vệ đồ án)
- File `.avi` / `.mp4` (video demo, nếu có)

Đặt tên file theo quy định của khoa hoặc tên gợi nhớ (ví dụ `BaoCao_BaoVe_220064.pptx`).
