@model List<ASPNET_DK24TTC6_LuuThiNgoc_CafeShop.Models.Product>
@{ ViewData["Title"] = "Inventory"; Layout = "~/Views/Shared/_AdminLayout.cshtml"; }

<div class="admin-page-header">
    <div>
        <h1 class="admin-page-title">Inventory</h1>
        <p class="admin-page-subtitle">Manage your roastery product catalog.</p>
    </div>
    <a asp-action="Create" class="admin-btn admin-btn--primary"><i class="fas fa-plus"></i> New Product</a>
</div>

<div class="admin-table-controls">
    <div class="admin-search-box">
        <i class="fas fa-search"></i>
        <input type="text" placeholder="Search products..." id="productSearch" />
    </div>
    <span style="font-size:0.85rem;color:#7A7A7A;">@Model.Count products</span>
</div>

<div class="admin-table-wrap">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Product</th>
                <th>Category</th>
                <th>Price</th>
                <th>Stock</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <div class="admin-product-cell">
                            <div class="admin-product-thumb">
                                @if (!string.IsNullOrEmpty(item.ImageUrl))
                                {
                                    <img src="@item.ImageUrl" alt="@item.Name" />
                                }
                                else
                                {
                                    <div class="admin-product-thumb--placeholder"><i class="fas fa-coffee"></i></div>
                                }
                            </div>
                            <div>
                                <p class="admin-product-name">@item.Name</p>
                                <p class="admin-product-sub">ID: @item.Id</p>
                            </div>
                        </div>
                    </td>
                    <td>@item.Category?.Name</td>
                    <td style="font-weight:600;">@item.Price.ToString("N0")đ</td>
                    <td>@item.Stock</td>
                    <td>
                        @if (item.IsActive)
                        {
                            <span class="status-dot status-dot--active">Active</span>
                        }
                        else
                        {
                            <span class="status-dot status-dot--inactive">Inactive</span>
                        }
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="admin-btn admin-btn--ghost" title="Edit"><i class="fas fa-pen"></i></a>
                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this product?')">
                            <button class="admin-btn admin-btn--ghost" style="color:#E53935;" title="Delete"><i class="fas fa-trash-alt"></i></button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
